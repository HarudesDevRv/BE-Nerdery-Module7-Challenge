# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ProductImage {
  imageId: ID!
  url: String!
}

type ProductWithDetails {
  productId: ID!
  name: String!
  description: String!
  images: [ProductImage!]!
  category: String!
  brand: String!
  price: Float!
  salePrice: Float!
  stock: Float!
  likesCount: Float!
}

type Category {
  name: String!
  description: String!
  imageUrl: String!
}

type Inventory {
  inventoryId: ID!
  productId: ID!
  storeId: ID!
  price: Float
  salePrice: Float
  stock: Float
  isActive: Boolean!
}

type ManagerProduct {
  productId: ID!
  name: String!
  description: String!
  images: [ProductImage!]!
  categoryId: ID!
  brandId: ID!
  inventories: [Inventory!]!
}

"""Upload custom scalar type"""
scalar Upload

type Query {
  products(filter: ProductFilterInput): [ProductWithDetails!]!
  productDetail(productId: ID!): ProductWithDetails!
  managerProducts(filter: ProductFilterInput): [ManagerProduct!]!
  productCategories: [Category!]!
}

input ProductFilterInput {
  category: String
  page: Int = 1
  limit: Int = 10
}

type Mutation {
  createProduct(input: CreateProductInput!): ManagerProduct!
  updateProduct(productId: ID!, input: UpdateProductInput!): ManagerProduct!
  deleteProduct(productId: ID!): Boolean!
  uploadProductImage(productId: String!, input: UploadImageInput!): ProductImage!
  deleteProductImage(imageId: ID!): Boolean!
  toggleLike(productId: ID!, likeStatus: Boolean!): Boolean!
}

input CreateProductInput {
  name: String!
  description: String!
  categoryId: ID!
  brandId: ID!
}

input UpdateProductInput {
  name: String
  description: String
  categoryId: ID
  brandId: ID
  isActive: Boolean
}

input UploadImageInput {
  file: Upload!
}